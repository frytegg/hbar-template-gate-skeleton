# JSON-RPC endpoint the in-process Hardhat network forks (chain and test scripts). Live networks ignore it.
HEDERA_RPC_URL=https://testnet.hashio.io/api

# Don't fill manually: the hardhat:account:generate and hardhat:account:import scripts write it.
DEPLOYER_PRIVATE_KEY_ENCRYPTED=
