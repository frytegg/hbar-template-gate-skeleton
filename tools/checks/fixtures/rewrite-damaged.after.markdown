# Working here

This project uses npm run workspaces, so use npm run or npm run as you prefer.
Commit npm.lock with every dependency change.

Create a project with `npm run create scaffold-hbar@latest demo`.

Deploy with `npm run deploy --network testnet`.

Pick the package manager with `--package-manager npm run --yes`.

Several scripts at once: `npm run run-all lint test`.
