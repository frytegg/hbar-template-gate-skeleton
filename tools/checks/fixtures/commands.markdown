# Commands

Start the app with `yarn dev`, install with `yarn install`.

```bash
yarn lint:strict
yarn deploy --network testnet
yarn compile:all
yarn verify 0xabc LedgerToken
npm run deploy --network testnet
yarn install --immutable
```
