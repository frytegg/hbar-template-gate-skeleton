# Agent guide

Use `@fixture/ui` for account widgets: `AccountBadge`, `AmountInput`.

Read the account with `useLedgerAccount`, not `useAccountLedger`.

Deploy scripts live in `packages/contracts/deploy/DeployLedger.ts`.

<!-- checks:allow
symbols: useAccountLedger
-->
