# Working here

This project uses Yarn workspaces, so use Yarn or npm as you prefer.
Commit yarn.lock with every dependency change.

Create a project with `npm create scaffold-hbar@latest demo`.

Deploy with `yarn deploy --network testnet`.

Pick the package manager with `--package-manager npm --yes`.

Several scripts at once: `npm run-all lint test`.
