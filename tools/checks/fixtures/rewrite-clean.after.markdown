# Working here

Install with `npm install`, then start the app:

```bash
npm run dev
npm run lint:strict
```

In CI the lockfile is enforced: `npm install`.
Both package managers offered by the CLI work; npm, too.
