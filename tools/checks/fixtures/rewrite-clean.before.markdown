# Working here

Install with `yarn install`, then start the app:

```bash
yarn dev
yarn lint:strict
```

In CI the lockfile is enforced: `yarn install --immutable`.
Both package managers offered by the CLI work; npm, too.
